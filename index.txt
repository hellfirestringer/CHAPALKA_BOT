<!DOCTYPE html><html lang="ru"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no,viewport-fit=cover"><title>GSE</title><link href="https://googleapis.com" rel="stylesheet"><link rel="stylesheet" href="style.css"></head><body><div class="game-container" id="stage"><div class="blood-flash" id="b-flash"></div><div class="bg-layer"></div><div class="bg-overlay"></div><div class="game-interface" id="click-gate"><div class="top-block"><h1 class="header-title" id="hotline-text">Продуктовый<br>магазин</h1><div class="combo-meter" id="combo-ui">x0</div></div><div class="stats-dashboard"><div class="stat-card left"><span class="stat-lbl">УДАРЫ</span><span class="stat-num" id="score-val">0</span></div><div class="stat-card right"><span class="stat-lbl">СКИДКА</span><span class="stat-num"><span id="discount-val">0</span>%</span></div></div></div></div><script>
const textContainer=document.getElementById('hotline-text'),textStr=textContainer.innerHTML;textContainer.innerHTML='';let delay=0;textStr.split('<br>').forEach((part,index)=>{part.split('').forEach(char=>{const span=document.createElement('span');span.innerHTML=char===' '?'&nbsp;':char;span.style.animationDelay=`${delay}s`;delay+=.08;textContainer.appendChild(span)});if(index===0)textContainer.appendChild(document.createElement('br'))});const stage=document.getElementById('stage'),scoreVal=document.getElementById('score-val'),discountVal=document.getElementById('discount-val'),comboUi=document.getElementById('combo-ui'),bFlash=document.getElementById('b-flash'),clickGate=document.getElementById('click-gate');let score=localStorage.getItem('gse_score')?parseInt(localStorage.getItem('gse_score')):0,combo=0,comboTimer=null;function updateMatchStats(){scoreVal.textContent=score.toLocaleString();let d=Math.floor(score/200);discountVal.textContent=d>10?10:d}updateMatchStats();
function triggerComboBonus(type,count){let i=0;function spawn(){if(i>=count||combo===0)return;createTarget(type);i++;setTimeout(spawn,150)}spawn()}
function addCombo(){combo++;clearTimeout(comboTimer);comboUi.textContent=`x${combo}`;comboUi.classList.add('active','bump');setTimeout(()=>comboUi.classList.remove('bump'),100);if(combo===20)triggerComboBonus('target',12);if(combo===35)triggerComboBonus('mega_stone',8);comboTimer=setTimeout(resetCombo,2500)}function resetCombo(){combo=0;comboUi.classList.remove('active')}function handleMiss(){resetCombo()}clickGate.addEventListener('touchstart',handleMiss);clickGate.addEventListener('click',handleMiss);const sfxHit=new Audio('hit.mp3'),sfxAlarm=new Audio('alarm.mp3'),sfxBg=new Audio('sample.mp3');sfxBg.loop=true;let audioUnlocked=false;function initAudio(){if(audioUnlocked)return;sfxBg.volume=1.0;sfxHit.volume=0.50;sfxAlarm.volume=0.50;sfxBg.play().catch(()=>{});audioUnlocked=true}function playSound(type){initAudio();const track=type==='alarm'?sfxAlarm:sfxHit;track.currentTime=0;track.play().catch(()=>{})}function createDecorItem(){const item=document.createElement('div');item.className='decor-item';const size=Math.floor(Math.random()*20)+26;item.style.width=item.style.height=size+'px';item.style.opacity=(Math.random()*.4+.2).toFixed(2);const startX=Math.random()*(stage.clientWidth-46);let currentY=-120,currentRotation=Math.random()*360;const speed=Math.random()*1.5+2,rotationSpeed=(Math.random()*2-1)*2;item.style.transform=`translate3d(${startX}px,${currentY}px,0) rotate(${currentRotation}deg)`;stage.insertBefore(item,clickGate);function fall(){if(!stage.contains(item))return;currentY+=speed;currentRotation+=rotationSpeed;item.style.transform=`translate3d(${startX}px,${currentY}px,0) rotate(${currentRotation}deg)`;if(currentY>stage.clientHeight+120)item.remove();else requestAnimationFrame(fall)}requestAnimationFrame(fall)}function createTarget(type='target'){const el=document.createElement('div'),isBoss=(type==='boss'),isMegaStone=(type==='mega_stone'),isKroks=(type==='kroks');if(isBoss)el.className='shoe-item boss-shoe';else if(isMegaStone)el.className='mega-stonek';else if(isKroks)el.className='kroks-danger';else el.className='shoe-item target-shoe';const sizeOffset=isKroks?176:(isMegaStone?180:(isBoss?160:112)),w=stage.clientWidth||window.innerWidth,h=stage.clientHeight||window.innerHeight;const randX=Math.random()*(w-sizeOffset),randY=isKroks?(Math.random()*(h-sizeOffset-360)+160):-190,currentRotation=Math.random()*360;el.style.left=randX+'px';el.style.top=randY+'px';if(!isKroks){var speed=isMegaStone?(h+200)/300:(isBoss?(Math.random()*3+4.5):(Math.random()*2+3.5));var rotationSpeed=isMegaStone?.5:(isBoss?(Math.random()*2-1)*3:(Math.random()*2-1)*2);el.style.transform=`rotate(${currentRotation}deg)`}
stage.insertBefore(el,clickGate);if(isKroks){setTimeout(()=>{if(stage.contains(el))el.remove()},4000)}function handleHit(clientX,clientY){if(!stage.contains(el))return;if(isKroks){playSound('alarm');resetCombo();bFlash.classList.add('active');stage.classList.add('screen-shake');setTimeout(()=>{bFlash.classList.remove('active');stage.classList.remove('screen-shake')},400);score=0;localStorage.setItem('gse_score',0);updateMatchStats();el.remove();return}playSound('hit');addCombo();let m=1;if(combo sleeve>=15)m=4;else if(combo>=10)m=3;else if(combo>=5)m=2;if(combo>=15)m=4;else if(combo>=10)m=3;else if(combo>=5)m=2;const bp=isMegaStone?10:(isBoss?5:1),points=bp*m;score+=points;localStorage.setItem('gse_score',score);updateMatchStats();const rect=stage.getBoundingClientRect(),clickX=clientX-rect.left,clickY=clientY-rect.top,punch=document.createElement('div');punch.className=isMegaStone?'punch-effect punch-ultra':(isBoss?'punch-effect punch-mega':'punch-effect');punch.style.left=clickX+'px';punch.style.top=clickY+'px';stage.appendChild(punch);setTimeout(()=>punch.remove(),400);const plus=document.createElement('div');plus.className='floating-plus';plus.textContent=m>1?`+${points} x${m}`:`+${points}`;if(isBoss||isMegaStone)plus.style.color='#ff0055';plus.style.left=clickX+'px';plus.style.top=(clickY-25)+'px';stage.appendChild(plus);setTimeout(()=>plus.remove(),400);el.remove()}el.addEventListener('touchstart',e=>{e.preventDefault();e.stopPropagation();initAudio();for(let i=0;i<e.changedTouches.length;i++)handleHit(e.changedTouches[i].clientX,e.changedTouches[i].clientY)},{passive:false});el.addEventListener('click',e=>{e.stopPropagation();initAudio();handleHit(e.clientX,e.clientY)});var fallY=randY;function fall(){if(!stage.contains(el)||isKroks)return;fallY+=speed;el.style.top=fallY+'px';if(fallY>h+190)el.remove();else requestAnimationFrame(fall)}requestAnimationFrame(fall)}setInterval(createDecorItem,450);function loopSpawn(type,minTime,maxTime){createTarget(type);setTimeout(()=>loopSpawn(type,minTime,maxTime),Math.random()*(maxTime-minTime)+minTime)}setTimeout(()=>loopSpawn('target',1200,2500),1000);setTimeout(()=>loopSpawn('boss',8000,20000),5000);setTimeout(()=>loopSpawn('mega_stone',21000,39000),15000);function loopSpawnKroks(){createTarget('kroks');if(Math.random()<0.3){setTimeout(()=>createTarget('kroks'),Math.floor(Math.random()*800)+400)}setTimeout(loopSpawnKroks,30000)}setTimeout(loopSpawnKroks,15000);function triggerShoeAvalanche(){let count=0;function spawnNext(){if(count>=15)return;const types=['target','target','boss','mega_stone'];createTarget(types[Math.floor(Math.random()*types.length)]);count++;setTimeout(spawnNext,200)}spawnNext()}setInterval(triggerShoeAvalanche,120000);
</script></body></html>
